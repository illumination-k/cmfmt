extern crate pulldown_cmark;
extern crate anyhow;
extern crate mecab;

use anyhow::Result;
use pulldown_cmark::{Parser, Options};

fn main() -> Result<()> {
    println!("Hello, world!");
    Ok(())
}
